<!--components/BlockBox/BlockBox.wxml-->
<view class="blockBox">
  <view wx:if="{{title}}" class="title">{{title}}</view>
  <view class="content" style="padding:{{padding}}">
    <slot></slot>
  </view>
</view>
